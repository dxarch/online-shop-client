import {RegisterInterface} from "../app/auth/types/register.interface";

export interface RegisterFormProps {
    // onSubmitHandler: (input: RegisterInterface) => void;
    // fetchingErrors: string | null;
    // fetchingPending: boolean;
}